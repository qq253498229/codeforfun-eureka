eureka:
  client:
    fetch-registry: ${REGISTRY:false}
    register-with-eureka: ${REGISTRY:false}
    service-url:
      default-zone: ${REGISTRY_URL:http://localhost:8761/eureka/}
spring:
  application:
    name: ${SPRING_APPLICATION_NAME:eureka}
